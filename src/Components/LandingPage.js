import React from "react"
//maybe getData() from weblisttoday endpoint

export default function LandingPage() {
   return (
      <div>
         <h2>Welcome!</h2>
         <h4>Featured Today</h4>
         {/* Check data for what we might use */}
         <p>Show component|| Image || ratings || title ? </p>
         <p>Show component here</p>
         <p>Show component here</p>
         <h4>Recently viewed shows</h4>
         <p>Recently viewed item</p>
         <p>Recently viewed item</p>
      </div>
   )
}
